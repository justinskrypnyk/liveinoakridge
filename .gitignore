# Environment variables — never commit
.env
.mcp.json

# Build output
dist/
.astro/

# Dependencies
node_modules/

# OS
.DS_Store

# Logs
*.log

# Local Netlify folder
.netlify

# Claude Code local settings — can capture command text (including secrets
# typed/echoed during a session) verbatim into the permission allowlist;
# never belongs in a shared/public repo
.claude/settings.local.json
src/data/listings.json
